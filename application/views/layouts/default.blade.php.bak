<!DOCTYPE html>
	@include('partials.header',array('title'=>$title))
    <body>

        <!-- This code is taken from http://twitter.github.com/bootstrap/examples/hero.html -->

	@include('partials.navbar',array('nav_active'=>$nav_active))

	{{ $content }}

	@include('partials.globaljs')
    </body>
</html>
